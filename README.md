# dev-tools

## dev-vm

Vagrantfile + setup script for dev VM (currently based on Fedora 3.4) with all packages, repos and setup for Kubernetes and OVN-Kubernetes development, testing in Kind + other miscellaneous utilities
